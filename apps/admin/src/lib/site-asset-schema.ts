import type { SiteAssetSlotKey } from "@/lib/types";

export const siteAssetLabels: Record<SiteAssetSlotKey, string> = {
  "home.heroImage": "Главная · hero image",
  "about.portraitImage": "Биография · portrait image",
};
